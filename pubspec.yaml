name: utopia_wm
description: Pangolin-desktop wm interface and hierarchy provider
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0-0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  
  provider: ^5.0.0
  dahlia_backend:
    #path: ../dahlia_backend/
    git:
      url: https://github.com/dahliaOS/backend.git
      ref: feature/pinnedApps

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  fonts:
    - family: CustomIcons
      fonts:
        - asset: fonts/CustomIcons.ttf